(* -------------------------------------------------------------------------- *)
(* --- Application Configuration                                          --- *)
(* -------------------------------------------------------------------------- *)

let app = ref "wcaml"
let name = ref "WCaml"
let version = ref "0.1"
let app_url = ref "app.wcaml.org"
let app_file = ref "org.wcaml.app"
